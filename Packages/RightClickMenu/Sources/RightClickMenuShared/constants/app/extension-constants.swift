/// Fixed facts about a file's extension.
public enum ExtensionConstants {

    /// The character between a file's name and its extension.
    public static let dot = "."

    /// The longest extension the editor will take. Real ones are far shorter.
    public static let maximumLength = 16
}
